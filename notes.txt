# Init project 
npm i -g @nestjs/cli
nest new backend-medifollow

---> npm run lint
---> npm run format

# Install config module for file .env 
npm install @nestjs/config

# Install mongoose : (mongobd)
npm i @nestjs/mongoose mongoose
npm ls @nestjs/mongoose mongoose @nestjs/core

# Auth JWT + Passport
npm install @nestjs/jwt passport-jwt @nestjs/passport passport

# Hash des mots de passe
npm install bcryptjs
npm install --save-dev @types/bcrypt
// securite : k yekteb mot de passe ghalet  b @IP kadeh men marra yetkass alih mel main.ts w temchi tetbloca accès f authservice
npm i express-rate-limit

//cron activation /déactivation 
npm install @nestjs/schedule

"NB ‼ :
Middleware = portique d’entrée (log, headers, contrôle basique)
JwtAuthGuard = carte d’identité (token valide ?)
RolesGuard = agent de sécurité (role correct ?)"


//commande yahya
npm install typeorm reflect-metadata
npm i mongodb@^6
npm i typeorm mongodb @nestjs/typeorm reflect-metadata class-validator class-transformer
npm i class-validator class-transformer
npm i typeorm reflect-metadata --legacy-peer-deps //ken 3andek error fel typeorm

//commandes Nour
/* push sur git*/
 git init
 git add .
 git commit -m "schema users"
 git branch -M signup
 git push -u origin signup


 #Khalil forgetpassword
npm install nodemailer bcrypt



................./--------------
npm install @nestjs/mapped-types


http://localhost:3000/api
npm install @nestjs/swagger swagger-ui-express


npm install pdfmake exceljs
npm install --save-dev @types/pdfmake


---------------------------------------------------------

//Commande Merge Ma branche --> Main

# Te placer sur main
git checkout main

# Mettre main à jour
git pull origin main

# Fusionner ta branche
git merge ta_branche
// NB !! : vérifier GitHUb desktop pour visualiser les erreurs afin de les acceptés 
yahyakaroui4TWIN233
# Pousser vers GitHub
git push origin main


qsdqsc
-------------------------------------------------------

// Commande Merge Main --> ta branche 
# Te placer sur ta branche
git checkout ta_branche

# Mettre à jour main depuis GitHub
git fetch origin

# Fusionner main dans ta branche
git merge origin/main

git push origin ta_branche
yahya

//pour statistique installez:
npm install chart.js ng2-charts@5 --legacy-peer-deps


//install twilio
 npm install twilio

//pour alert (detection anomalie)
//installer:
npm install @nestjs/axios axios


yahya



//Assurer la haute disponibilité de mon serveur avec utilisation de docker 
docker run -d --name medifollow-mongo --restart unless-stopped -p 27017:27017 -v medifollow-mongo-data:/data/db mongo:7

//visibilité sur le windows il faut stopper le mongo local 
net stop MongoDB

// Se connecter base de données du container 
mongodb://localhost:27017

// ou bien essayer de savoir l'IP du container pour se connecter 
docker inspect medifollow-mongo --format "{{.NetworkSettings.Networks.bridge.IPAddress}}"

// un petit test dans le CMD docker exec -it medifollow-mongo mongosh medifollow --eval "db.roles.countDocuments()"


SDs

// test unitaire de module : Service npx jest src/service/ --coverage --coverageReporters=text